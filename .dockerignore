# Root build context dipakai backend/Dockerfile (lihat docker-compose.prod.yml)
# dan docker/Dockerfile.web. Hanya yang di-COPY yang wajib ADA:
# backend/{package,src,tsconfig,Dockerfile} + database/ (backend),
# pubspec.yaml + lib/ + web/ + assets/ + docker/nginx-web.conf (web).
# Kecualikan sisanya agar transfer context tetap kecil.
.git
.dart_tool
.flutter-plugins-dependencies
.metadata
build
coverage
android
ios
linux
macos
windows
test
backend/node_modules
backend/dist
backend/uploads
backend/.env
node_modules
uploads
.env
.env.prod
.env.local
.opencode
*.log
